#include <iostream>
void main()
{
	double k = 6, r = 5e-7, x = 0.095, t, u;
	t = exp(c * x) / y + 3;
	u = sqrt(pow(z, 3) - 0.1);

	std::cout << "u=" << u << std::endl;
	std::cout << "t=" << t << std::endl;
}
